version: '3.9'

services:
  api-gateway:
    build:
      context: ./api-gateway
    ports:
      - '3000:3000'
    volumes:
      - ./api-gateway:/app
      - /app/node_modules
    env_file:
      - ./api-gateway/.env