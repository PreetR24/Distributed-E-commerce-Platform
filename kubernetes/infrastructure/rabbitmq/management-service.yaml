apiVersion: v1
kind: Service

metadata:
  name: rabbitmq-management
  namespace: dcp
  labels:
    app: rabbitmq
    component: message-broker

spec:
  type: ClusterIP

  selector:
    app: rabbitmq

  ports:
    - name: management
      port: 15672
      targetPort: management