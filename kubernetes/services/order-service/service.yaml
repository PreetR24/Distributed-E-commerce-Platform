apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: dcp

spec:
  type: ClusterIP

  selector:
    app: order-service

  ports:
    - name: http
      port: 4004
      targetPort: 4004

    - name: grpc
      port: 50051
      targetPort: 50051