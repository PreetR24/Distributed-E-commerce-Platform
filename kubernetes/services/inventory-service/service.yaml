apiVersion: v1
kind: Service

metadata:
  name: inventory-service
  namespace: dcp

spec:
  type: ClusterIP

  selector:
    app: inventory-service

  ports:
    - name: http
      port: 4006
      targetPort: 4006

    - name: grpc
      port: 50053
      targetPort: 50053