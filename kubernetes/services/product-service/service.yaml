apiVersion: v1
kind: Service

metadata:
  name: product-service
  namespace: dcp

spec:
  type: ClusterIP

  selector:
    app: product-service

  ports:
    - name: http
      port: 4002
      targetPort: 4002

    - name: grpc
      port: 50052
      targetPort: 50052